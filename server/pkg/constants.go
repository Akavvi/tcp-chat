package pkg

const (
	PORT   = ":9000"
	TYPE   = "tcp"
	PREFIX = "/"
	CREATE = PREFIX + "create"
	JOIN   = PREFIX + "join"
	LEAVE  = PREFIX + "leave"
	HELP   = PREFIX + "help"
	LIST   = PREFIX + "list"
)
